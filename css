body {
  background: #000;
  font-family: -apple-system, BlinkMacSystemFont, sans-serif;
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

.calculator {
  width: 320px;
  background: #000;
  border-radius: 28px;
  padding: 20px;
}

.screen {
  color: white;
  text-align: right;
  margin-bottom: 20px;
}

.display {
  font-size: 48px;
  min-height: 60px;
  overflow: hidden;
}

.history {
  font-size: 14px;
  opacity: 0.6;
  margin-top: 6px;
}

.digit {
  display: inline-block;
  height: 56px;
  overflow: hidden;
}

.digit-inner {
  display: block;
  transform: translateY(-56px);
  animation: scrollDigit 0.18s ease-out forwards;
}

@keyframes scrollDigit {
  to {
    transform: translateY(0);
  }
}

.buttons {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 12px;
}

button {
  height: 60px;
  border-radius: 50%;
  border: none;
  font-size: 20px;
  background: #333;
  color: white;
}

button:active {
  filter: brightness(1.3);
}

.zero {
  grid-column: span 2;
  border-radius: 30px;
}
